export const SubscribeResponse = {
    "type": "object",
    "properties": {
        "evt": {
            "type": "string",
            "description": "event name now subscribed to"
        }
    },
    "required": [
        "evt"
    ]
};
